import express from 'express';
import { authenticate } from '../middlewares/authMiddleware.js';

const router = express.Router();

/**
 * Rutas de usuario básicas (pueden expandirse según necesidad)
 */

export default router;

